Drop real item photos here, named to match the `photos` arrays in
`items.json` (e.g. `001-chest-front.jpg`). Empty for now — the site shows
a "photo coming soon" placeholder for any item whose photo file is missing.
